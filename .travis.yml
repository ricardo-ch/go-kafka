language: go

go:
  - "1.12.x"

notifications:
  email: false

before_install:
  - go get github.com/mattn/goveralls

script:
  - dep ensure
  - make build
  - make test
  - $GOPATH/bin/goveralls -service=travis-ci -ignore "mocks/*"